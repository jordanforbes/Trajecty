<%= form_with(model: truck_job) do |form| %>
  <% if truck_job.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(truck_job.errors.count, "error") %> prohibited this truck_job from being saved:</h2>

      <ul>
        <% truck_job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :truck_id, style: "display: block" %>
    <%= form.number_field :truck_id %>
  </div>

  <div>
    <%= form.label :job_id, style: "display: block" %>
    <%= form.number_field :job_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
